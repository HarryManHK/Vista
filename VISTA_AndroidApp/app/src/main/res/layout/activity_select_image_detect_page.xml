<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".SelectImageDetectPage">

    <!-- Image View for Displaying Image -->
    <ImageView
        android:id="@+id/imgResult"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/txtResultScroll"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.5"
    app:layout_constraintWidth_percent="0.9"
    tools:srcCompat="@tools:sample/avatars" />

    <!-- ScrollView to Make TextView Scrollable -->
    <android.widget.ScrollView
        android:id="@+id/txtResultScroll"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.4"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/imgResult">

        <!-- TextView for displaying the result -->
        <TextView
            android:id="@+id/txtResult"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="start"
            android:maxLines="10"
            android:padding="8dp"
            android:scrollHorizontally="false"
            android:text="@string/selectImageDetectPage_result"
            android:textColor="#3BA7C9"
            android:textSize="20sp" />
    </android.widget.ScrollView>

    </androidx.constraintlayout.widget.ConstraintLayout>